import SwiftUI

enum AppFont {

    // MARK: - Properties

    static let display: Font = .system(size: 28)
    static let title: Font = .system(size: 22)
    static let headline: Font = .system(size: 18)
    static let body: Font = .system(size: 16)
    static let caption: Font = .system(size: 14)
}
