# Product Analytics

Приложение использует Google Analytics for Firebase для измерения основных
пользовательских сценариев. Firebase подключён через IDFA-free Swift Package
product `FirebaseAnalyticsCore`; версия SDK зафиксирована на `12.17.0` в
XcodeGen-конфигурации.

## События приложения

| Событие | Параметры | Когда отправляется |
| --- | --- | --- |
| `calculation_attempted` | `outcome`: `completed`, `missing_required_input` или `invalid_duration` | После нажатия кнопки расчёта и определения результата валидации |
| `history_opened` | Нет | При явном открытии истории пользователем |
| `history_entry_reused` | Нет | После успешного выбора существующей записи истории |
| `result_export_started` | Нет | При нажатии кнопки экспорта PDF |

Firebase также может автоматически собирать стандартные события и технические
свойства приложения и устройства, например `first_open` и `session_start`.
Автоматическая отчётность по экранам, IDFV и сигналы персонализации рекламы
отключены в `Info.plist`.

## Запрещённые данные

В события нельзя добавлять:

- суммы инвестиций, пополнений или результаты расчёта;
- процентную ставку или точный срок инвестирования;
- UUID записей истории, user ID, IDFA и другие устойчивые идентификаторы;
- свободный текст, геолокацию или дополнительные данные устройства.

Новый параметр или событие требует отдельной проверки цели, минимизации данных
и актуальности App Store privacy disclosure.

## Настройка Firebase

1. Создайте iOS app в Firebase project с bundle identifier приложения.
2. Скачайте `GoogleService-Info.plist` из Firebase Console.
3. Добавьте файл в `Compound Interest/Resources/Plist/` и в app target.
4. Запустите `Scripts/generate.sh`, затем проверьте Debug-сборку и события в
   Firebase DebugView.

Если `GoogleService-Info.plist` отсутствует или настроен некорректно, это
считается ошибкой интеграции Firebase. В таком состоянии аналитика не должна
тихо деградировать: проблему нужно исправить в конфигурации проекта и
подтвердить через DebugView.
